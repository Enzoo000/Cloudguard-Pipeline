# A dedicated ServiceAccount instead of using "default" — least
# privilege starts here: this identity gets exactly the RBAC
# permissions granted in k8s/policy/rbac.yaml, which is none.

apiVersion: v1
kind: ServiceAccount
metadata:
  name: northbound-app
  namespace: northbound
